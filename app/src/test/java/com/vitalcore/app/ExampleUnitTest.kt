package com.vitalcore.app

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder unit test so the `test` and CI lint/test tasks have something
 * real to run from Milestone 1 onward. Replaced/extended with calculation
 * engine tests starting Milestone 4 (baseline) and 5-7 (Recovery/Sleep/Strain).
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
