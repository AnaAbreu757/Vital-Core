<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Health Connect permissions are declared as intent filters + runtime permission
         strings (androidx.health.connect.HealthPermissions.*) requested at runtime.
         No dangerous permissions are declared here beyond what Health Connect requires. -->

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <!-- Only used by the optional, user-configured AI Coach (ai/RemoteAiProvider) to
         reach the endpoint the user enters in Settings > AI Coach. No other network
         calls exist anywhere in the app. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".VitalCoreApplication"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.VitalCore">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.VitalCore">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Allows Health Connect to show this app as a data-viewing destination -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>

            <!-- Strava OAuth redirect (see integrations/strava/StravaAuthManager).
                 Matches the "vitalcore" scheme registered as the Authorization
                 Callback Domain in your Strava API application settings. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="${stravaRedirectScheme}" android:host="strava-callback" />
            </intent-filter>
        </activity>

        <activity-alias
            android:name=".HealthConnectPermissionsRationaleActivity"
            android:exported="true"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity-alias>

        <!-- Disable WorkManager's default initializer: VitalCoreApplication
             provides its own Configuration via Configuration.Provider so that
             Hilt-injected Workers (see notifications/) work correctly. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>
</manifest>
